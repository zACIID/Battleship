/* To learn more about this file see: https://angular.io/config/tsconfig. */
{
    "extends": "./tsconfig.json",
    "compilerOptions": {
        "outDir": "./out-tsc/spec",
        "types": ["jest", "@types/jest"],

        // This is needed because we didn't write the server code with strict enabled
        "strict": false
    },
    "include": [
        "src/**/*.d.ts",
        "src/**/*.spec.ts",
        "tests"
    ]
}
